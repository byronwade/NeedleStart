export function MapPreview() {
  return (
    <section>
      <h2>Lumina Map Preview</h2>
      <p>
        The current map output connects routes to source files, layouts, render-mode evidence, and
        generated artifacts so inspect commands can explain why a route exists.
      </p>
    </section>
  );
}
