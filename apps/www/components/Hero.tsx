export function Hero() {
  return (
    <section>
      <p>App-graph-native React framework</p>
      <h1>Your app ships with a map.</h1>
      <p>
        Lumina is being built to make routes, render modes, generated artifacts, and agent-safe
        inspection explicit from the first local workflow.
      </p>
    </section>
  );
}
