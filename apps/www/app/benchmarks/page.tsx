export default function BenchmarksPage() {
  return (
    <main>
      <h1>Benchmarks</h1>
      <p>
        Lumina has benchmark skeletons for route discovery, manifest size, graph queries, and
        adapter dispatch. They currently report not implemented until measured fixtures exist.
      </p>
      <p>
        This page is a status surface, not a performance claim or framework comparison.
      </p>
    </main>
  );
}
