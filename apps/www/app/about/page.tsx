export default function AboutPage() {
  return (
    <main>
      <h1>Why Lumina Exists</h1>
      <p>
        Lumina is being built for React applications that need route structure, render behavior,
        generated artifacts, and agent context to stay visible as the codebase grows.
      </p>
      <p>
        The current MVP work is focused on deterministic app discovery, compact manifests, and
        inspection commands before runtime behavior expands.
      </p>
    </main>
  );
}
