# tiny-static

Status: Implemented.

Purpose: stable future fixture for route discovery, manifest shape, and static request-path smoke checks.

Current behavior: contains one deterministic `app/page.tsx` route. It is used by `lumina bench route-discovery --json --run` to collect local raw route-discovery timing metadata.

It is not public comparison evidence.
